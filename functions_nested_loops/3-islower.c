#include "main.h"

/**
 * _islower - function that checks for lowercase character.
 *
 * Return: Always 0.
 */
int _islower(int r)
{
if (r <= 'z' && r >= 'a')
{
	return (1);
}
else
{

	return (0);
}
}
